# Ignorar claves privadas y certificados autofirmados (NUNCA subir a repositorio)
# Cada desarrollador debe generar su propio par (server.crt + server.key)
*.key
private.key
server.key
*.crt
certificate.crt
server.crt

# Ignorar archivos temporales
*.pem
*.der
*.pfx
*.p12
*.csr

